# ============================================================================
# FARMBOT NOVA FRONTEND - ENVIRONMENT VARIABLES
# ============================================================================
# Setup Guide: See README.md for detailed instructions
# Author: Aryan Kumar
# ============================================================================

# ----------------------------------------------------------------------------
# BACKEND API CONFIGURATION (Required)
# ----------------------------------------------------------------------------
# URL of the FarmBot Nova backend server
# Local development: http://localhost:8000
# Production: https://your-backend-url.com

VITE_BACKEND_API_URL=http://localhost:8000

# API Key for backend authentication
# Get this from your backend administrator or generate using:
# POST /admin/generate-api-key with X-Admin-Secret header

VITE_BACKEND_API_KEY=your-backend-api-key-here

# ----------------------------------------------------------------------------
# OPENWEATHER API KEY (Optional - Backend handles this now)
# ----------------------------------------------------------------------------
# Only needed if frontend makes direct weather API calls
# Most weather data is now fetched through the backend

VITE_OPENWEATHER_API_KEY=your-openweather-api-key-here

# ----------------------------------------------------------------------------
# SUPABASE CONFIGURATION (Required)
# ----------------------------------------------------------------------------
# Get from: https://supabase.com → Project Settings → API
# Free tier: 500MB database + 2GB transfer
# Used for: Response caching, manual entries, sensor data

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# ----------------------------------------------------------------------------
# OPTIONAL: SITE METADATA
# ----------------------------------------------------------------------------
# Used for OpenRouter API attribution

VITE_SITE_URL=https://yourfarm.app
VITE_SITE_NAME=Smart Farm Assistant

# ============================================================================
# EXTERNAL APIs (No key required - Public APIs)
# ============================================================================
# These are used automatically by the system:
#
# 1. SoilGrids API (ISRIC)
#    - URL: https://rest.isric.org/soilgrids/v2.0
#    - Data: Soil pH, NPK, organic carbon, texture
#    - Rate limit: Public, no authentication
#
# 2. Wikidata SPARQL
#    - URL: https://query.wikidata.org/sparql
#    - Data: Crop information, scientific names
#    - Rate limit: Public, rate-limited by IP
#
# 3. FAO Crop Database (Fallback)
#    - Hardcoded common crop information
#    - Used when APIs are unavailable
#
# ============================================================================
# SETUP VERIFICATION
# ============================================================================
# After setting up, verify with:
#
# 1. npm run dev
# 2. Open http://localhost:5173
# 3. Click "Smart AI" tab
# 4. Test query: "What crops can I grow in my area?"
#
# If successful, you should see:
# - Location detected
# - Thinking animation
# - Response with real data sources
#
# ============================================================================
# TROUBLESHOOTING
# ============================================================================
#
# Issue: "Backend API error" or connection refused
# Fix: Ensure VITE_BACKEND_API_URL points to your running backend server
#
# Issue: "Unauthorized" or 401 errors
# Fix: Verify VITE_BACKEND_API_KEY matches a valid key in backend API_KEYS
#
# Issue: Supabase connection failed
# Fix: Check VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY
#
# Issue: Variables not loading
# Fix: Restart dev server after changing .env
#
# ============================================================================
# SECURITY NOTES
# ============================================================================
# ⚠️  NEVER commit this file to version control
# ⚠️  All keys should be kept secret
# ⚠️  Use different keys for development and production
# ⚠️  Rotate keys periodically for security
#
# Add to .gitignore:
# .env
# .env.local
# .env.production
# ============================================================================
