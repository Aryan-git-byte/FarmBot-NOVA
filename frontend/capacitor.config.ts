import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aryan.farmmonitor',
  appName: 'Farm Monitoring System',
  webDir: 'dist'
};

export default config;
